# -*- mode: snippet -*-
#name : class ... { ... }
# --
/// ${1:$(yas/substr text "[^: ]*")}.
class ${1:Name}
{
    /** \name Ctor & dtor.
     ** \\{ */
  public:
    /// Construct ${1:$(if (string-match "^[aeiou]" text) "an" "a")} ${1:$(yas/substr text "[^: ]*")}.
    ${1:$(yas/substr text "[^: ]*")} ($2);
    /// Destroy ${1:$(if (string-match "^[aeiou]" text) "an" "a")} ${1:$(yas/substr text "[^: ]*")}.
    virtual ~${1:$(yas/substr text "[^: ]*")} ();
    /** \\} */
};
